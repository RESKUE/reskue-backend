apiVersion: v1
kind: Service
metadata:
  name: rescue_keycloak
  labels:
    app: rescue_keycloak
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: http
  selector:
    app: rescue_keycloak